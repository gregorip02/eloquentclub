import GuestLayout from "@/Layouts/GuestLayout";
import { Link } from "@inertiajs/react";

export const Index = () => {
    return (
        <GuestLayout>
        </GuestLayout>
    );
};

export default Index;
